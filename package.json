{
  "name": "health-scanner",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back-end,front-end -c blue,green \"npm run dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "install:all": "npm install --prefix back-end && npm install --prefix front-end"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
